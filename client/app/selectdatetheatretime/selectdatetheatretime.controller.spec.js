'use strict';

describe('Component: SelectdatetheatretimeComponent', function () {

  // load the controller's module
  beforeEach(module('yeomanApp'));

  var SelectdatetheatretimeComponent;

  // Initialize the controller and a mock scope
  beforeEach(inject(function ($componentController) {
    SelectdatetheatretimeComponent = $componentController('selectdatetheatretime', {});
  }));

  it('should ...', function () {
    expect(1).to.equal(1);
  });
});
